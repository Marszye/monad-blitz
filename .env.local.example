# Wallet address that receives x402 payments on Monad testnet
PAY_TO_ADDRESS=0xYourWalletAddress

# Private key of the deployer wallet (needs testnet MON for gas).
# This wallet also becomes the ApiRegistry `recorder` on deploy.
# NEVER commit the real value — this file only holds a placeholder.
PRIVATE_KEY=0xYourPrivateKey
